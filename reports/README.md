# Generated reports

Run `python -m src.report --db data/retail.duckdb --out reports` to generate aggregate CSV tables and `summary.json`. Generated files are excluded from Git; the repository includes the code and metric definitions required to reproduce them.
